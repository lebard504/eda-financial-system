version: '3.8'

services:

  customer-service:
    build: .
    container_name: customer-service
    ports:
      - "8082:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - account-service_default

networks:
  account-service_default:
    external: true